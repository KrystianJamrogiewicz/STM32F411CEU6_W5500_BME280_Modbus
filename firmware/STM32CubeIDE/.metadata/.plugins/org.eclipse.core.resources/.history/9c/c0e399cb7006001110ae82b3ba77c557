#include "app.h"
#include <stdio.h> // Potrzebne do sprintf
#include <string.h> // Potrzebne do strlen

// Zadeklarowanie zmiennych globalnych z innych plików
extern UART_HandleTypeDef huart1;

void App_Init(void)
{
    // Tu będzie start W5500 i BME280
    char msg[] = "[APP] Inicjalizacja zakonczona.\r\n";
    HAL_UART_Transmit(&huart1, (uint8_t*)msg, strlen(msg), 100);
}

void App_Loop(void)
{
    // Tu będzie obsługa Modbusa
    HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13);
    HAL_Delay(500);
}
